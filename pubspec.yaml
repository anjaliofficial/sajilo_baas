name: sajilo_baas
description: "A new Flutter project for sajilo_baas."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.0
  permission_handler: ^11.3.1
  web_socket_channel: ^2.4.0

  # Local Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2

  # Utilities & Functional Programming
  uuid: ^4.3.3
  equatable: ^2.0.7
  dartz: ^0.10.1
  flutter_riverpod: ^3.1.0 # ✅ keep only this
  pretty_dio_logger: ^1.4.0
  shared_preferences: ^2.5.4
  connectivity_plus: ^7.0.0
  flutter_secure_storage: ^10.0.0
  dio_smart_retry: ^7.0.1
  dio: ^5.9.0
  shimmer: ^2.0.0
  pull_to_refresh: ^2.0.0
  flutter_rating_bar: ^4.0.0
  socket_io_client: ^2.0.3
  fluttertoast: ^8.2.8
  gal: ^2.3.2
  flutter_map: ^8.0.0
  latlong2: ^0.9.1
  geolocator: ^10.1.0
  maps_launcher: ^3.0.0
  sensors_plus: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

  hive: ^2.2.3
  hive_flutter: ^1.1.0
  web_socket_channel: ^2.4.0
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.10

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
