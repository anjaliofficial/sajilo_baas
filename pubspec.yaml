name: sajilo_baas
description: "A new Flutter project for sajilo_baas."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Local Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2

  # Utilities & Functional Programming
  uuid: ^4.3.3
  equatable: ^2.0.7
  dartz: ^0.10.1 # Very useful for handling Failures vs Success in Login
  flutter_riverpod: ^3.1.0
  pretty_dio_logger: ^1.4.0
  shared_preferences: ^2.5.4
  connectivity_plus: ^7.0.0
  flutter_secure_storage: ^10.0.0
  dio_smart_retry: ^7.0.1
  dio: ^5.9.0
  riverpod: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Fixed: Only one instance of flutter_lints
  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
