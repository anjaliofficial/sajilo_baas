import 'package:flutter/material.dart';

class PlaceholderReviewsPage extends StatelessWidget {
  const PlaceholderReviewsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reviews')),
      body: const Center(child: Text('Reviews page coming soon!')),
    );
  }
}
