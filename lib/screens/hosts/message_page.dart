import 'package:flutter/material.dart';

class HostMessagesPage extends StatelessWidget {
  const HostMessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Messages Page'));
  }
}
